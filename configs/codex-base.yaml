job_name: codex-base
jobs_dir: jobs
n_attempts: 1
n_concurrent_trials: 1
environment:
  type: docker
agents:
  - name: codex
    model_name: openai/gpt-5.5
    env:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
    kwargs:
      reasoning_effort: high
